class Query < Source

  # has_one :indicator
  # has_many :parameters
  
  # accepts_nested_attributes_for :parameters, 
  #   :reject_if => lambda { |p| p[:name].blank? and p[:default_value].blank?}

end
